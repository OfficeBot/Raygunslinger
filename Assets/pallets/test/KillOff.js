#pragma strict
// var anim : AnimationClip;
function TriggerEnd() {
	Destroy (gameObject);
}

function Start () {
		// var anim = gameObject.GetComponent(Animator);
		// Debug.Log(anim.GetCurrentAnimationClipState(0));
	// Debug.Log(anim.speed);
	// var animator = gameObject.GetComponent(Animator);
	// var animationState = animator.GetCurrentAnimatorStateInfo(0);
	// Debug.Log(animationState.length);
	// yield WaitForSeconds (animationClipObject.length);
	// Destroy (gameObject);
	// var animator = gameObject.GetComponent(Animator);
	// animationController = animator.runtimeAnimatorController;
}

function Update () {

}